# Model provider: openai or demo
MODEL_PROVIDER=openai

# Keep this file secret-free. Put the real key in .env or your shell environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Optional OpenAI-compatible API base URL. The /responses path is appended.
# OPENAI_BASE_URL=https://api.openai.com/v1
